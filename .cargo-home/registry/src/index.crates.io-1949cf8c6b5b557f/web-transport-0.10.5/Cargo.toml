# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "web-transport"
version = "0.10.5"
authors = ["Luke Curley"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Generic WebTransport API with native (web-transport-quinn) and WASM (web-transport-wasm) support."
readme = "README.md"
keywords = [
    "quic",
    "http3",
    "webtransport",
]
categories = [
    "network-programming",
    "web-programming",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/moq-dev/web-transport"

[lib]
name = "web_transport"
path = "src/lib.rs"

[dependencies.bytes]
version = "1"

[dependencies.thiserror]
version = "2"

[dependencies.url]
version = "2"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies.web-transport-quinn]
version = "0.11.9"

[target.'cfg(target_arch = "wasm32")'.dependencies.web-transport-wasm]
version = "0.5.7"
