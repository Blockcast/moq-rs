# M.4 T1.7 stage 3 — session handoff (2026-05-30)

**Status: DONE + shipped.** Live single-pipe E2E works end to end:
real `moq_mmt` capture → `moq-pub-mmtp` (Mapping B) → `moq-relay-ietf` →
Shaka MSF in headless Chrome over real WebTransport → observe dump
(5 Init + 59 MFU, FI-ordered). Both verification vehicles green.

Full detail in memory `t1.7-stage3-live-e2e-recipe.md`.

## Branches (all pushed) + PRs
| Repo | Branch | PR | base | state |
|---|---|---|---|---|
| moq-rs | `blo-4020-m4-t1` | [Blockcast/moq-rs#6](https://github.com/Blockcast/moq-rs/pull/6) | `blo-4020-m1` | OPEN |
| shaka-player | `blo-4020-m4-t1-shaka-mmtp` | [Blockcast/shaka-player#3](https://github.com/Blockcast/shaka-player/pull/3) | `blockcast/main` | **MERGED** |
| moqcast-draft | `blo-4020-m4-mmt-receiver-contracts` | [kkroo/moqcast-draft#2](https://github.com/kkroo/moqcast-draft/pull/2) | `feat/group-align-formula` | OPEN |

(moq-rs origin = cloudflare READ-ONLY; push to `blockcast` remote only.)

## This session's work (committed)
- **moq-rs:** `eea732e` web-transport bump (Chrome-146 capsule fix: proto 0.5→0.6,
  quinn 0.11.4→0.11.9); `1b744b2` catalog under `catalog`+`.catalog`;
  `614123d` standalone E2E smoke+capture; `d6b23ff` Karma wrapper+CORS;
  `8a3b705` add `mmtp` packaging value.
- **shaka-player:** `37dd48f81` object_id delta reconstruction (T1.5b);
  `d88d3a3c2` observe harness page; `2b321704a` Karma spec; `8e10ffcfe` object_id
  unit test; `24d0d727b` read codec from selectionParams.codec.
- **moqcast-draft:** `193c590` catalog name=`catalog` (msf §5.2); `1d58b77`
  schema `packaging`+`selectionParams.codec`.

## Decisions resolved against IETF specs
- Catalog track name = **`catalog`** (draft-ietf-moq-msf §5.2 MUST); `.catalog` legacy alias.
- Container field = **`packaging`** (catalog-format; +`mmtp`), not `container`.
- Codec = **`selectionParams.codec`** (nested), not top-level.

## Verification (all green)
moq-transport 114 · moq-pub-mmtp 44 · moq-catalog 24 · shaka.msf 130 ·
live E2E `.planning/m4-t1.7-e2e.sh` exits 0 · eslint clean.

## OPEN items
1. **Ally (code-reviewer agent) review on moq-rs #6** — webhook re-fired via
   close→reopen; awaiting Ally's async consolidated comment. Check #6 comments.
2. **draft #2 Ally review** — `kkroo/moqcast-draft` is a personal repo likely
   OUTSIDE Ally's GitHub webhook → won't auto-trigger. Needs webhook install or a
   manual Paperclip issue assigned to Ally (id `e0a5011d-5c94-4801-be52-64c14f98ac26`).
3. **moqcast-draft `.xml` regen** via `mmark` (not installed locally) so the
   rendered draft reflects the `.md` catalog name/schema notes.
4. Merge moq-rs #6 + draft #2 after review.

## Next M.4 T1 tasks (post-T1.7, from earlier handoff)
T1.2 (CMAF-wrap AVCC NAL MFUs → SegmentReference/playback, replaces observe-first),
T1.5c (timing/AV-sync), B-MIG-relay receive-path window wiring, T1.FEC, T1.VER,
T1.6 (@blockcast/transport WebTransport factory).
